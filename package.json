{
  "name": "blackoak",
  "version": "1.0.0",
  "main": "app.js",
  "license": "MIT",
  "dependencies": {
    "mocha": "^5.2.0",
    "mongoose": "^5.3.9",
    "node-schedule": "^1.3.0",
    "node-telegram-bot-api": "^0.30.0"
  },
  "scripts": {
    "start": "node app.js",
    "deploy": "now rm blackoak --yes && now --public && now alias",
    "watch": "nodemon app.js",
    "test": "mocha"
  }
}
