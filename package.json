{
  "name": "blackoak",
  "version": "1.0.0",
  "main": "app.js",
  "license": "MIT",
  "dependencies": {
    "mocha": "^5.2.0",
    "mongoose": "^5.2.4",
    "node-schedule": "^1.3.0",
    "node-telegram-bot-api": "^0.30.0"
  },
  "scripts": {
    "start": "node app.js",
    "deploy": "now rm blackoak --safe --yes && now --public && now alias",
    "watch": "nodemon app.js",
    "test": "mocha"
  }
}
